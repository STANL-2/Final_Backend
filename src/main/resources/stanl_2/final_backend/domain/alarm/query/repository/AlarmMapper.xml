<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="stanl_2.final_backend.domain.alarm.query.repository.AlarmMapper">
    <resultMap id="MemberAlarmsByNoCursorId" type="stanl_2.final_backend.domain.alarm.query.dto.AlarmQueryDTO">
        <id property="alarmId" column="ALR_ID"/>
        <result property="message" column="ALR_MSG"/>
        <result property="redirectUrl" column="ALR_URL"/>
        <result property="type" column="ALR_TYPE"/>
        <result property="readStatus" column="ALR_READ_STAT"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="memberId" column="MEM_ID"/>
    </resultMap>
    <select id="findAlarmsByMemberId" resultMap="MemberAlarmsByNoCursorId">
        SELECT
               a.alr_id,
               a.alr_msg,
               a.alr_url,
               a.alr_type,
               a.alr_read_stat,
               a.mem_id
          FROM tb_alarm a
         WHERE mem_id = #{memberId}
         ORDER BY CAST(SUBSTRING(ALR_ID, 5) AS UNSIGNED) DESC
         LIMIT #{size}
    </select>

    <resultMap id="MemberAlarmsByCursorId" type="stanl_2.final_backend.domain.alarm.query.dto.AlarmQueryDTO">
        <id property="alarmId" column="ALR_ID"/>
        <result property="message" column="ALR_MSG"/>
        <result property="redirectUrl" column="ALR_URL"/>
        <result property="type" column="ALR_TYPE"/>
        <result property="readStatus" column="ALR_READ_STAT"/>
        <result property="createdAt" column="CREATED_AT"/>
        <result property="memberId" column="MEM_ID"/>
    </resultMap>
    <select id="findAlarmsByMemberIdAndCursorId" resultMap="MemberAlarmsByCursorId">
        SELECT
               a.alr_id,
               a.alr_msg,
               a.alr_url,
               a.alr_type,
               a.alr_read_stat,
               a.mem_id
          FROM tb_alarm a
         WHERE mem_id = #{memberId}
           AND CAST(SUBSTRING(ALR_ID, 5) AS UNSIGNED) > #{cursorId}
         ORDER BY CAST(SUBSTRING(ALR_ID, 5) AS UNSIGNED) DESC
         LIMIT #{size}
    </select>
</mapper>